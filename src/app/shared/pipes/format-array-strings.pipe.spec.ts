import { FormatArrayStringsPipe } from './format-array-strings.pipe';

describe('FormatArrayStringsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatArrayStringsPipe();
    expect(pipe).toBeTruthy();
  });
});
